import PanierClient from './PanierClient';

export const metadata = {
  title: 'Panier — ILU SHOP',
  description: 'Votre panier ILU SHOP. Commande convenue par chat avec notre équipe.',
};

export default function PanierPage() {
  return <PanierClient />;
}
